# Deploy on autom8-accounting Railway service (NOT frontend VITE_*, NOT Munafe backend alone).
DATABASE_URL=postgresql+psycopg2://postgres:password@localhost:5432/autom8_accounting
INGEST_WEBHOOK_SECRET=change-me-shared-with-munafe-LEDGER_INGEST_SECRET
# Optional: allow insecure local without HMAC (never in production)
# ALLOW_INSECURE_INGEST=false
